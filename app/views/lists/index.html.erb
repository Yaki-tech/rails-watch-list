<div class="container mt-4">
  <h1>Mes listes</h1>

  <%= link_to "Créer une nouvelle liste", new_list_path, class: "btn btn-primary my-3" %>

  <div class="row">
    <% @lists.each do |list| %>
      <div class="col-12 col-md-4 mb-3">
        <div class="card">
          <div class="card-body">
            <h2 class="card-title"><%= list.name %></h2>
            <%= link_to "Voir la liste", list_path(list), class: "btn btn-outline-secondary btn-sm" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
